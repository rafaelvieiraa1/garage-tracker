services:
  db:
    image: postgres:15
    container_name: postgres_garage
    environment:
      POSTGRES_DB: garage_tracker
      POSTGRES_USER: rafael
      POSTGRES_PASSWORD: admin
    ports:
      - "5432:5432"